# Example environment variables (DO NOT put real secrets here)
# Copy this file to `.env` in the repo root and fill in your secret values.

# Perplexity API key - add the real key to your local `.env` (or export it in your shell)
PERPLEXITY_API_KEY=

# Optional: choose the LLM provider used locally (ollama, gemini, perplexity, mock)
LLM_PROVIDER=ollama

# Ollama model name to use when `LLM_PROVIDER=ollama` (example: mistral)
OLLAMA_MODEL=mistral

# Other app test/dev secrets (used in CI example)
SECRET_KEY=replace-me
SECURITY_PASSWORD_SALT=replace-me
