# This file includes global variables that will be available inside your project
# 1. In the front end code you can access these variables like this: console.log(import.meta.env.VARIABLE_NAME)
# 1. In the back end code you access the variable by importing os and then typing print(os.getenv('VARIABLE_NAME'))

# Back-End Variables
SECRET_KEY=
JWT_SECRET_KEY=
SECURITY_PASSWORD_SALT=
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
SMTP_USE_TLS=
FLASK_APP=src/app.py
FLASK_DEBUG=1
DEBUG=TRUE

# Front-End Variables
VITE_BASENAME=/
VITE_BACKEND_URL=http://localhost:5000
RL=
