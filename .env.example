# Example .env for Tapin — safe to commit
# Copy this to `.env` and fill real secrets locally or via your CI/secret store

# -------------------------
# Supabase
# -------------------------
SUPABASE_URL=https://<your-supabase-project>.supabase.co
SUPABASE_ANON_KEY=your-supabase-anon-key
SUPABASE_JWT_SECRET=your-supabase-jwt-secret

# -------------------------
# Database (Postgres)
# -------------------------
# Use session pooler URL recommended for Supabase; replace <PASSWORD>
SQLALCHEMY_DATABASE_URI=postgresql://postgres:<YOUR_DB_PASSWORD>@aws-1-us-east-1.pooler.supabase.com:5432/postgres?pool_mode=session
# Alternative direct connection (if you have IPv6 or direct host access)
# SQLALCHEMY_DATABASE_URI=postgresql://postgres:<YOUR_DB_PASSWORD>@db.<your-supabase-project>.supabase.co:5432/postgres

# For frameworks using DATABASE_URL env
DATABASE_URL=${SQLALCHEMY_DATABASE_URI}

# -------------------------
# Flask & Security
# -------------------------
FLASK_ENV=development
SECRET_KEY=replace-with-a-random-secret
JWT_SECRET_KEY=replace-with-a-random-jwt-secret
SECURITY_PASSWORD_SALT=replace-with-a-random-salt

# -------------------------
# LLM / Third-party APIs
# -------------------------
LLM_PROVIDER=mock    # use `mock` for local dev without keys
LLM_MODEL=gemini-2.5-flash-lite
PERPLEXITY_API_KEY=your-perplexity-api-key
GOOGLE_API_KEY=your-google-api-key
CUSTOM_SEARCH_ENGINE_ID=your-custom-search-engine-id

# -------------------------
# Ticketmaster (example)
# -------------------------
TICKETMASTER_API_KEY=your-ticketmaster-api-key
TICKETMASTER_CONSUMER_SECRET=your-ticketmaster-consumer-secret

# -------------------------
# Frontend / Misc
# -------------------------
VITE_API_URL=http://127.0.0.1:5000
CORS_ORIGINS=http://localhost:5173,http://localhost:3000,http://localhost:3001,http://127.0.0.1:5173

# -------------------------
# Notes
# -------------------------
# - Never commit your real `.env` with secrets. Use `.env.example` to document
#   required variables. Store real secrets in your OS environment, CI secrets,
#   or a secure vault.
# - After updating `.env`, restart your dev server so the new env vars are loaded.
