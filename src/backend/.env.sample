# Tapin Backend Environment Configuration
# Copy this file to .env and fill in your values

# =============================================================================
# Core Application Secrets (REQUIRED for production)
# =============================================================================
SECRET_KEY=your-secret-key-here-change-in-production
JWT_SECRET_KEY=your-jwt-secret-key-here
SECURITY_PASSWORD_SALT=your-password-salt-here

# =============================================================================
# Database Configuration
# =============================================================================
# For local development (SQLite):
SQLALCHEMY_DATABASE_URI=sqlite:///data.db

# For production (PostgreSQL/Supabase):
# SQLALCHEMY_DATABASE_URI=postgresql://user:pass@host:5432/dbname

# =============================================================================
# Event Discovery / LLM Configuration
# =============================================================================
# Provider Options: 'gemini', 'ollama', or 'mock' (Perplexity is optional/legacy)
# - 'gemini': Google Gemini API (recommended for production)
# - 'ollama': Local Ollama instance (for offline development)
# - 'mock': Returns sample data (for testing without API keys)
LLM_PROVIDER=gemini

# -----------------------------------------------------------------------------
# Google Gemini API (Production)
# -----------------------------------------------------------------------------
# Get your key at: https://aistudio.google.com/app/apikey
# Free tier: 60 requests/minute
# Cost: ~$0.075 per 1M tokens (very economical)
GEMINI_API_KEY=your-gemini-api-key-here
GEMINI_MODEL=gemini-2.5-flash-lite
# Alternative models: gemini-1.5-flash, gemini-1.5-pro

# -----------------------------------------------------------------------------
# Perplexity AI API (Alternative)
# -----------------------------------------------------------------------------
# Get your key at: https://www.perplexity.ai/settings/api
# Uncomment and set if using perplexity provider
# Perplexity entries are kept for legacy compatibility only. Commented out
# by default to avoid accidental usage in production. Uncomment if you have
# a current Perplexity key and intend to use that provider.
# PERPLEXITY_API_KEY=your-perplexity-api-key-here
# PERPLEXITY_MODEL=sonar

# -----------------------------------------------------------------------------
# Ollama (Local Development)
# -----------------------------------------------------------------------------
# Install: https://ollama.ai
# Pull a model: ollama pull mistral
# Runs locally without API keys (free, offline)
OLLAMA_MODEL=mistral
OLLAMA_BASE_URL=http://localhost:11434
# Alternative models: llama2, codellama, mixtral

# =============================================================================
# Ticketmaster API (Optional - for premium event discovery)
# =============================================================================
# Get your API key at: https://developer.ticketmaster.com/
# Leave blank if not using Ticketmaster integration
TICKETMASTER_API_KEY=your-ticketmaster-api-key-here

# =============================================================================
# Email Configuration (for password reset)
# =============================================================================
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_USE_TLS=true

# =============================================================================
# Frontend Configuration (for React app)
# =============================================================================
# Frontend: set `VITE_API_URL` in `src/front/.env` so the React app points to
# the correct backend API URL for local development and production.
# Example (local): VITE_API_URL=http://127.0.0.1:5000
# Example (production): VITE_API_URL=https://your-app.fly.dev
