# Tapin Environment Configuration (SAMPLE)
# Copy this file to `.env` and fill with real values locally. Do not commit `.env`.

# LLM Provider (choose: mock, google, gemini, ollama)
LLM_PROVIDER=mock
LLM_MODEL=gemini-2.5-flash-lite

# API Keys (placeholders) - replace locally in a private `.env`
GOOGLE_API_KEY=REDACTED_GOOGLE_API_KEY
CUSTOM_SEARCH_ENGINE_ID=REDACTED_CUSTOM_SEARCH_ENGINE_ID

# Ticketmaster (example placeholder)
TICKETMASTER_API_KEY=REDACTED_TICKETMASTER_API_KEY
TICKETMASTER_CONSUMER_SECRET=REDACTED_TICKETMASTER_CONSUMER_SECRET

# Supabase placeholders
SUPABASE_URL=REDACTED_SUPABASE_URL
SUPABASE_ANON_KEY=REDACTED_SUPABASE_ANON_KEY
SUPABASE_JWT_SECRET=REDACTED_SUPABASE_JWT_SECRET

# Database (use secure connection string in your local .env)
SQLALCHEMY_DATABASE_URI=REDACTED_DATABASE_URI
DATABASE_URL=REDACTED_DATABASE_URL

# Flask & security
FLASK_ENV=development
SECRET_KEY=REDACTED_SECRET_KEY
JWT_SECRET_KEY=REDACTED_JWT_SECRET_KEY
SECURITY_PASSWORD_SALT=REDACTED_SECURITY_PASSWORD_SALT

# Cache & performance
CACHE_EXPIRY_DAYS=30

# Frontend
VITE_API_URL=http://127.0.0.1:5000

# CORS origins
CORS_ORIGINS=http://localhost:5173,http://localhost:3000
# ================================
# Tapin Backend Configuration
# ================================
# Copy this file to .env and replace with your actual values
# DO NOT commit .env to version control!

# Flask & JWT Secrets (REQUIRED - change in production!)
SECRET_KEY=dev-secret-key-change-in-production-use-random-string
JWT_SECRET_KEY=dev-jwt-secret-change-in-production-use-random-string
SECURITY_PASSWORD_SALT=dev-salt-change-in-production-use-random-string

# Database Configuration
# For local development, SQLite is used by default
# For production, use PostgreSQL or MySQL
SQLALCHEMY_DATABASE_URI=sqlite:///backend/data.db
# Example PostgreSQL: postgresql://user:password@localhost:5432/tapin
# Example MySQL: mysql://user:password@localhost:3306/tapin

# SMTP Configuration (Optional - for password reset emails)
# Leave blank if you don't need email functionality in development
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-email@example.com
SMTP_PASS=your-app-password
SMTP_USE_TLS=true

# API Configuration (Optional)
# VITE_API_URL=http://localhost:5000/api
# CORS_ORIGINS=http://localhost:3000

# Development vs Production
FLASK_ENV=development
# FLASK_ENV=production

# Logging
# LOG_LEVEL=INFO
